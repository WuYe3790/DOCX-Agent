# DOCX Agent v2 依赖
# 安装: pip install -r requirements.txt

# === 核心: LLM + DOCX 操作 ===
openai>=1.0          # LLM API 客户端 (DeepSeek / SenseNova / OpenAI 兼容)
lxml>=4.0            # DOCX XML 解析与构建
markdown-it-py>=3.0  # Markdown 解析 (compiler pipeline)
Pillow>=10.0         # 图片处理 (insert_image_after_paragraph)

# === v2 HTTP + WebSocket 服务 ===
fastapi>=0.100       # 后端 HTTP 框架
uvicorn>=0.22        # ASGI 服务器
websockets>=11.0     # WebSocket 协议支持
pydantic>=2.0        # FastAPI request/response 模型

# === v3 测试 (pytest 在 tests/ 早已用, 之前未声明, 顺手补上) ===
pytest>=8.0          # tests/ 目录下大量测试已用 pytest, 这次新增的 docx_preview_diff / word_preview 端点测试也用
httpx>=0.27          # fastapi TestClient 间接依赖, 端点测试需要

